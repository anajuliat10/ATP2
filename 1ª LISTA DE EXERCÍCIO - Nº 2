#include <stdio.h>

  int returnDobro (int A){
        return A * 2;
  }
  
  int main (){
      int numero;
      printf ("Digita aí que eu te devolvo o dobro:");
      scanf ("%d", &numero);
      printf ("O dobro de %d é: %d", numero, returnDobro(numero));
      
      return 0;
  }
