#include <stdio.h>

  void saudacao (char nome [50]){
      printf ("Olá, %s", nome);
    
      
  }
      int main (){
        char nome[50];
        printf ("Digite o nome:");
        scanf ("%s", nome);
        saudacao (nome);
    
        return 0;
      }
       
