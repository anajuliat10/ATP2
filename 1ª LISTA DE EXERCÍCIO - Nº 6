#include <stdio.h>

    void resultado (float media){
        if (media >= 6.0){
        printf ("Aprovado");
            
        }
        else {
            printf ("reprovado");
        }
    }
    
    int main (){
        float media;
        printf ("Digite a média:");
        scanf ("%f", &media);
        resultado(media);
        
        return 0;
    }
